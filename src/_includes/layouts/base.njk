<!doctype html>
<html {% if section %} data-current="{{ section }}"{% endif %} lang="en">

	{% include "components/head.njk" %}

	<body>

		<header class="container">
			{% include "components/nav.njk" %}
		</header>

		<main class="container padding-bottom-xl">
			{{ layoutContent | safe }}
		</main>
		
	</body>

</html>
